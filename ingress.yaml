---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: immich
spec:
  ingressClassName: traefik
  tls:
    - secretName: immich-sindt-org-tls
      hosts:
        - immich-dev.sindt.org
  rules:
  - host: immich-dev.sindt.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: immich-server
            port:
              number: 2283
